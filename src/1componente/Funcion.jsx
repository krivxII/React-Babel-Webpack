import React, { Component} from "react";
import {hot} from "react-hot-loader";

function Function() {
      return (
        <div>
          <h1>Componente funcion</h1>
        </div>
      );
    }

export default hot(module)(Function);